# Sentinel Shield v2.0.1 — Publishing Checklist (engine-only maintenance)

Do **not** publish until every box is checked and a separate explicit publish
authorization is given. This tool chain never tags or publishes automatically.

## Pre-tag gates (all must be READY)

- [x] Release source frozen: engine commit `32812ed43289104af61b0eb2fc20c784ca2b72c1`
- [x] Post-v2.0.0 diff is docs + SHA-pinned workflow-action upgrades only (no runtime change)
- [x] Default-branch CI green on the source (7 required workflows, event=push)
- [x] Static/lint clean: `sh -n`, `shellcheck -S error`, `actionlint`, `zizmor`
- [x] Artifacts: 16 CI artifacts digest-verified, owned, unexpired (0 failures)
- [x] Security acceptance: decision `accepted`, 0 blocking, 0 violations
- [x] Compatibility matrix: complete, nothing missing (bound to `32812ed`)
- [x] Adopter scorecard: `pass`
- [x] Upgrade validation: `pass` (2.0.0 → 2.0.1); Rollback validation: `pass`
- [x] Release manifest: self-consistency verified (body sha256 `8b7079a9…`)
- [x] Candidate `verify-candidate` for `stage=ga scope=engine-only` → **READY**
- [x] Negative controls: `framework-validated` GA → **BLOCKED**, `full-platform` GA → **BLOCKED**

## Publish gates (require separate explicit authorization)

Owner instruction alone is **not** sufficient. The release-authorization flow requires
both a governed authorization record (bound by a `--confirm-token`) **and** an explicit
destructive step (`--execute`); no tool creates a tag without it.

- [x] Evidence PR merged to `master` (PR #39, merge `1f12de3`)
- [x] Final tag target confirmed against merged evidence (metadata-only; target `32812ed`)
- [x] Explicit release-owner instruction to publish received
- [~] Governed two-person authorization record — **WAIVED** under the sole-maintainer path
      (no second GitHub login exists; the two-person `requested_by ≠ approved_by` control is
      structurally unsatisfiable). Recorded in `evidence/releases/v2.0.1-authorization-waiver.json`
      and [`docs/release-authorization-policy.md`](release-authorization-policy.md). The normal
      governed record would otherwise require:
      `authorize-production-release.sh authorize --candidate evidence/releases/v2.0.1-ga-candidate.json --source-commit <TAG_TARGET> --authorization <record> --confirm-token <token>`
      matching the candidate fingerprint (`candidate_hash`) and every identity field, with
      **distinct** `requested_by` / `approved_by` and a non-expired `expires_at`.
- [x] Signed tag created (SSH-signed by release owner):
      `git tag -s v2.0.1 32812ed43289104af61b0eb2fc20c784ca2b72c1 -m "Sentinel Shield v2.0.1 — Engine-Only Maintenance Release"`
- [x] Tag pushed
- [x] GitHub Release created from `docs/v2.0.1-release-notes.md`, marked latest (2026-07-09)

## Non-negotiable claim

Title: `Sentinel Shield v2.0.1 — Engine-Only Maintenance Release`.
Allowed: `engine-only maintenance release`. Forbidden: `framework-validated`,
`full-platform GA`, `Laravel validated`, `Symfony validated`,
`enterprise GA for all profiles`, `all frameworks production-ready`.
